---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <div class="min-h-screen flex items-center justify-center relative">
    <!-- 背景图片 -->
    <div
      class="absolute inset-0 bg-cover bg-center bg-no-repeat z-0"
      style={`background-image: url('/images/15.jpg');`}
    >
    </div>

    <!-- 半透明遮罩层 -->
    <div class="absolute inset-0 bg-black/50 z-10"></div>

    <!-- 内容区域 -->
    <div class="relative z-20 text-center px-4 py-16">
      <div class="max-w-3xl mx-auto">
        <h1 class="text-9xl font-bold text-white mb-6">404</h1>
        <p class="text-2xl md:text-3xl text-white font-medium mb-8">
          很奇怪，起司找不到你要的内容，可能被三体人删除了。
        </p>
        <a
          href="/"
          class="inline-block bg-gradient-to-r from-cheese-accent to-blue-600 hover:from-blue-700 hover:to-cheese-accent text-white font-medium py-3 px-8 rounded-full transition-all cursor-pointer"
        >
          返回首页
        </a>
      </div>
    </div>
  </div>
</Layout>
